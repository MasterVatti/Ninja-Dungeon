tree("Root")
    fallback
        tree("Attack")
        tree("Follow")

tree("Follow")
    while
        sequence
            not IsAtRequiredDistance(20.0)

        repeat
            sequence
                IsTherePlayer
                FollowPlayer

tree("Chase")
    sequence
            GetTarget
            while not IsTargetKilled
                sequence
                    Chase